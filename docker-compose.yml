services:
  py-scrape:
    build: .
    container_name: py-scrape
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      PYTHONUNBUFFERED: "1"
