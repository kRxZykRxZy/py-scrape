services:
  py-scrape:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/py_scrape.db
      HOST: 0.0.0.0
      PORT: 8000
    volumes:
      - ./data:/app/data
